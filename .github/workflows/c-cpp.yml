name: C/C++ CI

on:
  workflow_dispatch

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: install valgrind
      run: apt install -y valgrind
    - name: make
      run: make
    - name: make test
      run: make test
